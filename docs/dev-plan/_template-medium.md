# [板块短名] · [一句话目标，10-15 字]

<!--
  ============================================================================
  中度模板（适用 14/18 份板块）。
  约 200-350 行。
  保留所有 8 段，每段长度按右侧注释控制。
  填完后必须删掉所有 <!-- ... --> 注释。
  违反 README.md §2 全局约束的内容会被退回。
  ============================================================================
-->

| 元数据 | 值 |
|---|---|
| 周次 | W{n}.{m}                                  <!-- 必须 W3.1 这种格式 -->
| 优先级 | 必做 / 推荐 / 可选                          <!-- 三选一 -->
| 工作量 | N 工作日（或 N-M 工作日区间）                <!-- 实事求是，1-5 范围内 -->
| 依赖 | [板块名](相对路径), ...                       <!-- 没依赖填「无」 -->
| 启用后赋能 | [板块名](相对路径), ...                  <!-- 谁需要等本板块完成 -->
| 状态 | 📋 待开始                                 <!-- 见 README §2.13 -->

---

## 1. 目标与背景

<!--
  4 段，每段 2-4 行（共 50-80 行）：
  - 为什么做（业务/技术驱动）
  - 做完世界变成什么样（用户视角 + 开发者视角各 1 段）
  - 不在本板块范围（明确剔除项，避免 scope creep）
  - 关键 KPI 或验证指标（数字化）
-->

### 1.1 为什么做

[此处填：当前实现的缺陷或限制，为何这是 W{n}.{m} 必须解决的事；引用 README.md §3 依赖图说明上下游关系]

### 1.2 做完后

- **用户视角**：[填：终端用户/甲方看到什么变化]
- **开发者视角**：[填：可调用什么新接口、获得什么能力]

### 1.3 不做什么（剔除）

- [ ] [明确不在本板块范围的事项 1]
- [ ] [明确不在本板块范围的事项 2]

### 1.4 关键 KPI

- [指标 1：数字化目标，如「Bridge round-trip < 30s」]
- [指标 2]

---

## 2. 当前现状（起点）

<!--
  3 段，每段 5-10 行（共 30-60 行）：
  - 现有实现：引用具体文件 + 行号 + 关键代码片段（5-15 行）
  - 已知文件清单（要改/扩的）
  - 缺什么（bullet list，3-8 条）
-->

### 2.1 现有实现

主入口：[`相对路径`](../../相对路径) L{x}-L{y}

```startLine:endLine:相对路径
[5-15 行关键代码片段]
```

### 2.2 涉及文件清单

- 要改：
  - [`path/a.ts`](../../path/a.ts) - [改动要点]
  - [`path/b.yml`](../../path/b.yml) - [改动要点]
- 要建：
  - `path/new1.ts` - [用途]
  - `path/new2.sh` - [用途]

### 2.3 缺什么

- [ ] [缺失能力 1，描述 + 影响]
- [ ] [缺失能力 2]

---

## 3. 目标产物（终点）

<!--
  3 段（共 30-50 行）：
  - 新增文件列表（每行：路径 + 1 句话用途）
  - 修改文件列表（每行：路径 + 改动要点）
  - 新增能力清单（API endpoint / 合约方法 / env 变量 / verify 脚本 / Slack 通知模板填空）
-->

### 3.1 新文件

| 路径 | 用途 |
|------|------|
| `path/to/new1.ts` | [一句话] |
| `path/to/new2.sh` | [一句话] |
| `verify/X.sh` | 本板块验收脚本 |

### 3.2 修改文件

| 路径 | 改动要点 |
|------|---------|
| [`path/a.ts`](../../path/a.ts) | [bullet 简述] |
| [`path/b.yml`](../../path/b.yml) | [bullet 简述] |

### 3.3 新增能力

- **API endpoint**：`GET /api/x` → `{...}`
- **env 变量**：`X_FOO`、`X_BAR`（占位写进 `secrets.env.example`）
- **verify 脚本**：`bash verify/X.sh`
- **Slack 模板填空**：按 README §2.9 模板 A 填

---

## 4. 设计决策与已知坑

<!--
  4 段（共 40-80 行）：
  - 关键技术选型（为什么 A 不 B；引用上游文档）
  - 版本 pinning（npm @x.y.z / solc @x.y.z / docker image @tag）
  - 兼容性约束（不允许改的接口）
  - 已知坑（前期踩过 or 同类项目踩过的，每条要写"症状 + 规避"）
-->

### 4.1 关键选型

- **选 X 不选 Y**：[理由 + 引用 link]

### 4.2 版本 pinning

| 依赖 | 版本 | 理由 |
|------|------|------|
| `npm-package` | `1.2.3` | [为什么不用 latest] |
| `solc` | `0.8.24` | [兼容性] |
| `docker image` | `tag@sha256:xxx` | [复现性] |

### 4.3 兼容性约束

- [不允许破坏 X 接口（原因 + 谁会受影响）]
- [向前兼容旧 state.json 字段 Y]

### 4.4 已知坑

1. **[症状描述]** → 规避：[做法]
2. **[症状描述]** → 规避：[做法]

---

## 5. 实现步骤（按顺序）

<!--
  5-8 个 Step，每个 Step 30-60 行（共 150-300 行总）。
  每个 Step 标题必须以动词开头。
  每个 Step 末尾必须有"完成判定"（机器可验证）。
-->

### Step 1: [动词开头的短标题]

[3-5 行描述这一步做什么、为什么]

操作：
- a) [具体动作 1]
- b) [具体动作 2]

示例代码（如有）：

```ts
// 新代码 snippet（≤15 行；超长拆 Step）
export function foo() { /* ... */ }
```

**完成判定**：`bash -c "..."` 输出 `{...}` / 文件 `X` 存在 / `docker ps` 看到 `Y healthy`

### Step 2: ...

### Step 3: ...

### Step 4: ...

### Step 5: ...

<!-- 6-8 个 step 都按上面格式 -->

---

## 6. 验收

<!--
  4-6 条（共 30-50 行）。
  全部机器可验证。
  禁止"手动检查"。
-->

- [ ] `bash verify/X.sh` 全绿
- [ ] `forge test --match-contract X --match-test testY` 通过（如涉及合约）
- [ ] `curl http://{host}:{port}/healthz` 返回 `{"status":"ok"}`
- [ ] [端到端流程：浏览器/CLI 看到的具体输出]
- [ ] Prometheus `http://localhost:9090/api/v1/query?query={metric}` 返回 ≥ N
- [ ] Grafana dashboard `{name}` 全 panel 有数据 30 分钟以上

---

## 7. 风险与回退

<!--
  2-4 条风险（共 20-40 行）。
  每条：风险 + 概率 + 影响 + 降级方案。
  降级方案必须不阻塞下游板块。
-->

| 风险 | 概率 | 影响 | 降级方案 |
|------|------|------|---------|
| [风险 1] | 中 | 高 | [env 开关回退到 X 模式] |
| [风险 2] | 低 | 中 | [手动 fallback 步骤] |

---

## 8. 完成后必做

```bash
# 1. commit
git add . && git commit -m "feat(模块名): [一句话改动]"
git push

# 2. 更新进度
# - 改 docs/dev-plan/README.md §4 全局进度表本行
# - 在本板块文档 §Changelog 表格新增一行

# 3. Slack 通知（按 README §2.9 模板 A）
# 在 #progress 频道发送：
# ✅ [板块名] 完成 (W{周}.{子})
# - PR/Commit: <link>
# - 关键产物: <bullet 3 条>
# - 验收: bash verify/X.sh 全绿 ✓
# - 下一步赋能: <下游板块>

# 4. 如果加了新 stage（部署阶段）
# - 改 script/testnet/install.sh
# - 改 script/testnet/README.md
# - 改 script/testnet/doctor.sh
```

---

## 9. 参考

- 上游：[`org/repo`](https://github.com/...) §xxx 章节
- BSC 文档：[链接 + 章节]
- 项目内：[`相对路径`](../../相对路径)
- 历史决策：本板块 §Changelog 与 §6「关键决策」中记录

---

## Changelog

| 日期 | 变更 | 实施者 |
|------|------|--------|
| YYYY-MM-DD | 创建 | [name] |
