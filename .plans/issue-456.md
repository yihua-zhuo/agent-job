Now I have all the information I need. Here is the implementation plan:

# Implementation Plan — Issue #456

## Goal

Create ORM models for `Campaign`, `CampaignEvent`, and `Trigger` in `src/db/models/`, define the `EventType` enum alongside the existing marketing enums, and generate an Alembic migration so all three tables exist in the database with multi-tenant isolation and proper relationships.

## Affected Files

- `src/db/models/campaign.py` — **new** — ORM model for `campaigns` table
- `src/db/models/campaign_event.py` — **new** — ORM model for `campaign_events` table
- `src/db/models/trigger.py` — **new** — ORM model for `campaign_triggers` table
- `src/models/marketing.py` — **modify** — add `EventType` enum (`sent`, `opened`, `clicked`, `bounced`)
- `alembic/versions/<new>.py` — **new** — autogenerated migration for all three tables

## Implementation Steps

1. **Add `EventType` enum to `src/models/marketing.py`**: Add `class EventType(Enum)` with values `SENT = "sent"`, `OPENED = "opened"`, `CLICKED = "clicked"`, `BOUNCED = "bounced"`. Export it from `src/models/__init__.py`.

2. **Create `src/db/models/campaign.py`**: Define `CampaignModel` inheriting from `db.base.Base`. Table name `campaigns`. Columns: `id` (Integer PK), `tenant_id` (Integer, indexed), `name` (String 255), `type` (String 50, e.g. `email`/`sms`), `status` (String 50, e.g. `draft`/`active`), `subject` (String 255, nullable), `content` (Text), `target_audience` (Text, nullable), `trigger_type` (String 50, nullable), `trigger_days` (Integer, nullable), `created_by` (Integer), `sent_count` (Integer, default 0), `open_count` (Integer, default 0), `click_count` (Integer, default 0), `created_at`/`updated_at` (DateTime with server_default). Include `to_dict()` method.

3. **Create `src/db/models/campaign_event.py`**: Define `CampaignEventModel` inheriting from `db.base.Base`. Table name `campaign_events`. Columns: `id` (Integer PK), `tenant_id` (Integer, indexed), `campaign_id` (Integer FK to `campaigns.id`), `customer_id` (Integer), `event_type` (String 20), `created_at` (DateTime with server_default). Add composite index on `(tenant_id, campaign_id)`. Include `to_dict()` method.

4. **Create `src/db/models/trigger.py`**: Define `TriggerModel` inheriting from `db.base.Base`. Table name `campaign_triggers`. Columns: `id` (Integer PK), `tenant_id` (Integer, indexed), `campaign_id` (Integer FK to `campaigns.id`, nullable — trigger may exist without a campaign initially), `name` (String 255), `type` (String 50), `conditions` (JSON, default dict), `is_active` (Boolean, default True), `created_at`/`updated_at` (DateTime with server_default). Add composite index on `(tenant_id, campaign_id)`. Include `to_dict()` method.

5. **Auto-discoverability**: Because `src/db/models/__init__.py` uses `pkgutil.iter_modules` to auto-discover all `Base` subclasses, the new model files will be picked up automatically — no changes needed to `__init__.py`.

6. **Generate the Alembic migration**: Following the workflow in `CLAUDE.md`, spin up a clean `alembic_dev` database, run `alembic upgrade head`, then `alembic revision --autogenerate -m "add_campaign_tables"`. Review the generated file in `alembic/versions/<rev>_add_campaign_tables.py` to correct any issues (e.g. add composite indexes, fix server defaults, ensure CASCADE on FK). Run `alembic upgrade head && alembic downgrade -1 && alembic upgrade head` to verify. Run a second autogen as a drift check and delete it if empty.

## Test Plan

- Unit tests in `tests/unit/`: Create `tests/unit/test_campaign_model.py` with a `mock_db_session` fixture using a stateful handler that exercises `CampaignModel`, `CampaignEventModel`, and `TriggerModel` `to_dict()` output and verifies columns are serialized correctly.
- Integration tests in `tests/integration/`: Create `tests/integration/test_campaign_integration.py` using the `db_schema`, `tenant_id`, and `async_session` fixtures. Test creating a campaign, creating a trigger linked to it, and creating a campaign event — then verify counts and relations via the ORM. Run with `pytest tests/integration/test_campaign_integration.py -v`.

## Acceptance Criteria

- `from src.db.models import CampaignModel, CampaignEventModel, TriggerModel` imports succeed without error
- `alembic current` reports the new revision after running `alembic upgrade head`
- All three tables (`campaigns`, `campaign_events`, `campaign_triggers`) exist in the database and pass `\d <table>` inspection
- `CampaignModel.campaign_events` relationship (or equivalent FK query) returns linked events filtered by `tenant_id`
- Unit tests pass: `pytest tests/unit/ -v`
- Integration tests pass: `pytest tests/integration/test_campaign_integration.py -v`
