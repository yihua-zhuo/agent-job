Now I have everything I need. The `TenantModel` file already exists but is missing `slug` and `usage_limits` per the issue spec, and there are no pending migrations for it despite the head being at `9d8e7f6a5b3c_add_auth_tables`.

# Implementation Plan — Issue #471

## Goal
Add `slug` and `usage_limits` columns to `TenantModel` and generate a new Alembic migration to apply them as `NOT NULL` columns with appropriate server defaults to the `tenants` table. No service, router, or middleware code is involved.

## Affected Files
- `src/db/models/tenant.py` — add `slug` and `usage_limits` columns to `TenantModel`
- `src/db/models/__init__.py` — `TenantModel` already imported; no changes needed
- `alembic/env.py` — `TenantModel` already in `_ALEMBIC_MODEL_IMPORTS`; no changes needed
- `alembic/versions/<new_revision>_add_tenant_slug_and_usage_limits.py` — new migration generated by alembic, then reviewed and corrected

## Implementation Steps

1. **Update `src/db/models/tenant.py`**: Add `slug: Mapped[str]` and `usage_limits: Mapped[dict]` columns to `TenantModel` using `Mapped`, `mapped_column`, `String`, `JSON` from SQLAlchemy. `slug` is a non-nullable `String(100)` with a server default; `usage_limits` is a non-nullable `JSON` column with `default=dict`. Add both to `to_dict()` output.

2. **Spin up a clean `alembic_dev` database** as documented in CLAUDE.md:
   ```
   docker compose -f configs/docker-compose.test.yml up -d test-db
   docker exec configs-test-db-1 psql -U test_user -d postgres -c "DROP DATABASE IF EXISTS alembic_dev;"
   docker exec configs-test-db-1 psql -U test_user -d postgres -c "CREATE DATABASE alembic_dev;"
   ```

3. **Run all existing migrations against `alembic_dev`** to bring it to head:
   ```
   export PYTHONPATH=src
   export DATABASE_URL="postgresql+asyncpg://test_user:test_pass@localhost:5432/alembic_dev"
   alembic upgrade head
   ```

4. **Generate the new migration via autogenerate**:
   ```
   alembic revision --autogenerate -m "add tenant slug and usage_limits"
   ```

5. **Review the generated migration** (`alembic/versions/<id>_add_tenant_slug_and_usage_limits.py`):
   - Confirm the `upgrade()` block creates `slug` (String, server_default `''`) and `usage_limits` (JSON/JSONB) as NOT NULL columns on the `tenants` table
   - Confirm the `downgrade()` block drops both columns in reverse order
   - Ensure `down_revision` points to `9d8e7f6a5b3c` (current head)
   - Fill in any missing `server_default` values if autogenerate left them empty

6. **Verify the migration applies cleanly**:
   ```
   alembic upgrade head
   alembic downgrade -1
   alembic upgrade head
   ```

7. **Run a drift check** to confirm autogenerate produces an empty diff:
   ```
   alembic revision --autogenerate -m "drift_check"
   ```
   If the new file has `pass` in both `upgrade()` and `downgrade()`, delete it.

8. **Run linting**:
   ```
   ruff check src/ && ruff format --check src/
   ```

## Test Plan

- **Unit tests in `tests/unit/`**: Create `tests/unit/test_tenant_model.py`. Define a `MockState` with `tenant` state, a `make_tenant_handler(state)` in `tests/unit/conftest.py` following the existing pattern (factory + stateful, similar to `make_customer_handler`), and a `mock_db_session` fixture combining it. Test: constructing `TenantModel`, calling `to_dict()` with all fields including `slug` and `usage_limits`, and verifying the model has the correct column definitions. Mock DB only — no real SQL executed.
- **Integration tests in `tests/integration/`**: Create `tests/integration/test_tenant_integration.py`. Use `db_schema`, `tenant_id`, `async_session` fixtures. Test that the `tenants` table can be inserted with a non-null `slug` and `usage_limits`, and that selecting the row back yields the correct values. Use `pytest.raises(NotFoundException)` for any not-found error case if a service is later added.

## Acceptance Criteria

- `TenantModel` in `src/db/models/tenant.py` has `slug: Mapped[str]` and `usage_limits: Mapped[dict]` columns
- `to_dict()` on a `TenantModel` instance includes `slug` and `usage_limits` keys
- `TenantModel` is exported from `src/db/models/__init__.py` (already done — confirm)
- `TenantModel` appears in `alembic/env.py` `_ALEMBIC_MODEL_IMPORTS` (already done — confirm)
- A new alembic migration exists in `alembic/versions/` with `down_revision: 9d8e7f6a5b3c`, creates `slug` and `usage_limits` as NOT NULL, and has a correct `downgrade()` that drops both columns
- `alembic upgrade head` and `alembic downgrade -1` both succeed on `alembic_dev` with no errors
- A subsequent autogenerate produces an empty diff (no unapplied model changes remain)
