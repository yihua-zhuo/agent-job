Now I have a thorough understanding of the codebase. Here is the implementation plan:

# Implementation Plan — Issue #620

## Goal

Create the `agent_tasks` SQLAlchemy ORM model (`src/db/models/agent_tasks.py`) and an Alembic migration that creates the `agent_tasks` table with a composite index on `(task_id, tenant_id)`. The model must enforce multi-tenant isolation via the `tenant_id` column and expose a Python-level status enum. No service or router code is required.

## Affected Files

- `src/db/models/agent_tasks.py` — New file: `AgentTaskModel` ORM class
- `alembic/versions/<new_revision>.py` — New migration: creates `agent_tasks` table and index

## Implementation Steps

1. **Create `src/db/models/agent_tasks.py`**
   - Define a module-level `AgentTaskStatus` enum with values `"pending"`, `"dispatched"`, `"running"`, `"completed"`, `"failed"`.
   - Define `AgentTaskModel` extending `Base` with `__tablename__ = "agent_tasks"`.
   - Columns (in order):
     - `id: Mapped[int]` — `Integer`, primary key, autoincrement
     - `task_id: Mapped[str]` — `String(64)`, non-nullable, unique (the `'atask_'`-prefixed string generated by the caller)
     - `tenant_id: Mapped[int]` — `Integer`, non-nullable, `index=True` (also included in the composite index below)
     - `description: Mapped[str | None]` — `Text`, nullable
     - `status: Mapped[str]` — `String(50)`, non-nullable, default `"pending"`
     - `subtasks: Mapped[list]` — `JSONB` (from `sqlalchemy.dialects.postgresql`), default `list`, non-nullable
     - `created_at: Mapped[datetime]` — `DateTime(timezone=True)`, `server_default=func.now()`, non-nullable
     - `updated_at: Mapped[datetime]` — `DateTime(timezone=True)`, `server_default=func.now()`, `onupdate=func.now()`, non-nullable
   - Add `__table_args__` with `Index("ix_agent_tasks_task_id_tenant_id", "task_id", "tenant_id", unique=True)` for the required composite index.
   - Add a `to_dict()` method mirroring the pattern used by `TaskModel` and `AutomationLogModel` — include all columns; serialize `subtasks` as-is (JSON already).

2. **Create the Alembic migration**
   - Run `alembic revision --autogenerate -m "add_agent_tasks"` (requires a disposable `alembic_dev` database — see CLAUDE.md §Alembic Migrations for the setup commands).
   - The generated migration's `upgrade()` must contain `op.create_table('agent_tasks', ...)` with all columns above, including the composite unique index `ix_agent_tasks_task_id_tenant_id`. Use `postgresql.JSONB(astext_type=sa.Text())` for the `subtasks` column to match the ORM model's `JSONB` type.
   - The `downgrade()` must drop the table (Alembic will also handle the index automatically).
   - Verify the migration applies and downgrades cleanly: `alembic upgrade head`, `alembic downgrade -1`, `alembic upgrade head`.
   - Run a second empty autogen (`alembic revision --autogenerate -m "drift_check"`) to confirm no residual drift; delete the empty revision if both `upgrade()` and `downgrade()` are `pass`.

3. **Verify the model is registered**
   - The auto-discovery in `src/db/models/__init__.py` uses `pkgutil.iter_modules`, so no manual import is needed. Confirm `AgentTaskModel` is picklable / importable with `python -c "from db.models import AgentTaskModel; print(AgentTaskModel.__tablename__)"`.

## Test Plan

- Unit tests in `tests/unit/`: Create `tests/unit/test_agent_tasks_model.py` covering:
  - `AgentTaskStatus` enum has the five expected values.
  - `AgentTaskModel.__tablename__` equals `"agent_tasks"`.
  - `AgentTaskModel` has all seven data columns plus `id`.
  - `to_dict()` returns keys for every column and handles a model instance with `subtasks=None` gracefully.
  - The composite index `ix_agent_tasks_task_id_tenant_id` is present in `__table_args__`.
- Integration tests in `tests/integration/`: No integration tests required for this scope (no service layer yet).

## Acceptance Criteria

- `src/db/models/agent_tasks.py` exists and imports without error.
- `from db.models import AgentTaskModel` resolves to the correct class.
- The ORM model has `id`, `task_id`, `tenant_id`, `description`, `status`, `subtasks`, `created_at`, `updated_at` columns.
- `task_id` values carry an `'atask_'` prefix convention (enforced by callers, not the model).
- A composite unique index on `(task_id, tenant_id)` exists at the DB level.
- `tenant_id` has its own single-column index (in addition to the composite one).
- `status` is stored as `String(50)` (not a DB-level enum).
- `subtasks` is a JSONB column with a non-null default of `[]`.
- The migration upgrades and downgrades cleanly with no residual diff.
- `ruff check src/db/models/agent_tasks.py` passes with no errors.
