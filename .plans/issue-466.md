Now I have everything I need. Let me write the plan.

# Implementation Plan — Issue #466

## Goal

Define SQLAlchemy ORM models for Dashboard, Report, ChartData, ReportType (enum), and ChartType (enum) in `src/db/models/`, add them to the Alembic auto-discover path, and generate a migration. The schema is: Dashboard (id, tenant_id, name, description, created_at), Report (id, tenant_id, name, type, config JSON, created_at), ChartData (id, report_id, chart_type, data JSON). All types must be asyncpg-compatible.

## Affected Files

- `src/db/models/analytics.py` — Extend existing file: add `ReportType` and `ChartType` as `sqlalchemy.Enum`, add `chart_type` column to `ReportModel`, add new `ChartDataModel` table. Existing `DashboardModel` and `ReportModel` are already present and will not be moved.
- `src/models/analytics.py` — Add `ChartData` dataclass (currently missing; the file has `ReportType`, `ChartType`, `Dashboard`, `Report` but no `ChartData`).
- `alembic/env.py` — No structural change needed; `import db.models` at line 14 already auto-discovers all models in `src/db/models/` via `__init__.py`'s `pkgutil` loop, so the new model classes are picked up automatically.
- `alembic/versions/<new_revision>.py` — New migration file generated by `alembic revision --autogenerate`.
- `tests/unit/test_analytics_orm.py` — New file: unit tests for ORM model field values and `to_dict()` output.
- `tests/integration/test_analytics_integration.py` — New file: integration tests using `db_schema`, `tenant_id`, `async_session` fixtures.

## Implementation Steps

1. **Add SQLAlchemy enums and ChartDataModel to `src/db/models/analytics.py`**
   - Import `Enum` and `ForeignKey` from `sqlalchemy`, and `Enum` from `sqlalchemy.types` if needed.
   - Define `ReportType` as `sqlalchemy.Enum` with values `"sales_revenue"`, `"sales_conversion"`, `"customer_growth"`, `"customer_churn"`, `"pipeline_forecast"`, `"team_performance"`.
   - Define `ChartType` as `sqlalchemy.Enum` with values `"line"`, `"bar"`, `"pie"`, `"funnel"`, `"table"`.
   - In `ReportModel`: change `type: Mapped[str]` to `type: Mapped[str]` (keep String for migration flexibility; the enum values are enforced at the Python/service layer). Add `report_type: Mapped[str]` mapped_column (or keep `type` column, no structural change needed to existing `ReportModel`).
   - Add `ChartDataModel` class: `__tablename__ = "chart_data"`, columns: `id` (Integer PK), `report_id` (Integer, FK to reports.id, index=True), `chart_type` (String 50, nullable=False), `data` (JSON, default=dict, nullable=False), `created_at` (DateTime with server_default=func.now()). Add `report: Mapped["ReportModel"]` relationship with `back_populates="chart_data"`.
   - In `ReportModel`: add `chart_data: Mapped[list["ChartDataModel"]]` relationship with `back_populates="report"`, cascade all-delete-orphan, lazy raise.

2. **Add `ChartData` dataclass to `src/models/analytics.py`**
   - Add `@dataclass class ChartData` with fields: `labels: list[str]`, `datasets: list[dict]`, `chart_type: ChartType`.

3. **Generate the Alembic migration**
   - Spin up the test docker DB: `docker compose -f configs/docker-compose.test.yml up -d test-db`.
   - Create a disposable `alembic_dev` DB: `docker exec configs-test-db-1 psql -U test_user -d postgres -c "DROP DATABASE IF EXISTS alembic_dev;"` then `CREATE DATABASE alembic_dev;`.
   - Set env: `export PYTHONPATH=src` and `export DATABASE_URL="postgresql+asyncpg://test_user:test_pass@localhost:5432/alembic_dev"`.
   - Run `alembic upgrade head` against `alembic_dev`.
   - Run `alembic revision --autogenerate -m "add_dashboard_report_chartdata"` — this produces a new file in `alembic/versions/`.
   - Review the generated `upgrade()` and `downgrade()` functions: verify `chart_data` table has FK to `reports.id`, both tables have `tenant_id` index and composite index `(tenant_id, id)`. Adjust manually if autogenerate missed anything (e.g., composite indexes, server defaults).
   - Verify the migration applies and rolls back: `alembic upgrade head`, `alembic downgrade -1`, `alembic upgrade head`.
   - Run a second autogenerate against `alembic_dev` to confirm it is clean (empty diff); if so, the generated migration is final. If not, fix the migration.

4. **Add unit tests in `tests/unit/test_analytics_orm.py`**
   - Import from `db.models.analytics`: `DashboardModel`, `ReportModel`, `ChartDataModel`, `ReportType`, `ChartType`.
   - `TestDashboardModel`: construct `DashboardModel(id=1, tenant_id=1, name="Sales", description="Q1 sales", widgets=[], owner_id=1, is_default=False)` and assert `to_dict()` returns expected keys/values including ISO-formatted `created_at`.
   - `TestReportModel`: construct with `type="sales_revenue"` and assert `to_dict()`["type"] equals the input.
   - `TestChartDataModel`: construct with `chart_type="bar"` and `data={"labels":["Jan"],"datasets":[]}`. Assert `to_dict()`["chart_type"] equals `"bar"` and `data` equals the dict.
   - `TestEnumValues`: assert `ReportType.SALES_REVENUE.value == "sales_revenue"` and `ChartType.LINE.value == "line"`.

5. **Add integration tests in `tests/integration/test_analytics_integration.py`**
   - Use `@pytest.mark.integration` on the class.
   - `test_crud_dashboard`: create a Dashboard record in the DB, query it back, assert name and description match.
   - `test_crud_report`: create a Report, query back, assert `type` equals expected value.
   - `test_chartdata_belongs_to_report`: create a Report, create a ChartData record pointing to it via FK, query the chart_data row, assert `report_id` matches and `chart_type` equals expected value.
   - Use `db_schema` fixture for table creation/teardown and `async_session` for the DB session.

## Test Plan

- Unit tests in `tests/unit/test_analytics_orm.py`: construct DashboardModel, ReportModel, ChartDataModel directly and assert `to_dict()` output; assert enum `.value` strings.
- Integration tests in `tests/integration/test_analytics_integration.py`: CRUD on dashboards, reports, and chart_data tables via real async session; verify FK relationship between chart_data and reports.

## Acceptance Criteria

- `src/db/models/analytics.py` defines `ReportType`, `ChartType`, `DashboardModel`, `ReportModel`, and `ChartDataModel` — all importable without errors.
- `ChartDataModel` has a ForeignKey to `reports.id` and is reachable via `ReportModel.chart_data` relationship.
- `alembic revision --autogenerate` produces a non-empty migration with `CREATE TABLE chart_data` and appropriate indexes.
- `pytest tests/unit/test_analytics_orm.py -v` passes.
- `pytest tests/integration/ -m integration -v` passes against a live PostgreSQL database.
- `ruff check src/db/models/analytics.py` and `ruff format --check src/db/models/analytics.py` report no errors.

## Risks / Open Questions

- `src/models/analytics.py` (Pydantic/dataclass layer) already defines `ReportType` and `ChartType` as Python `Enum`. Adding SQLAlchemy `Enum` types in `src/db/models/analytics.py` creates two enum definitions. The service/router layer should consistently import from `src/models/analytics.py` for the enum values used in API contracts; the ORM layer uses the SQLAlchemy enum types only for column typing. This is an existing pattern in the codebase (Python enums for contracts, SQLAlchemy columns for persistence) and is acceptable — no rename needed.
- The existing `DashboardModel` in `src/db/models/analytics.py` has an `owner_id` and `is_default` column not listed in the issue's simple schema (id, tenant_id, name, description, created_at). Those columns should be preserved as they already exist in the model and no issue asks to remove them.
