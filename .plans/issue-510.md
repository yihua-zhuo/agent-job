Now I have everything needed. Let me write the plan.

# Implementation Plan — Issue #510

## Goal
Add a `CustomerEnrichment` ORM model and corresponding Alembic migration to persist third-party customer data enrichment records. The model stores enrichment provider, raw JSON payload, and refresh scheduling, linked to a customer via a foreign key with indexes for efficient querying.

## Affected Files
- `src/db/models/customer_enrichment.py` — New file: `CustomerEnrichment` ORM model (id, customer_id FK, provider, raw_data_json, enriched_at, next_refresh_at)
- `alembic/versions/<revision>_create_customer_enrichments.py` — New file: Alembic migration creating the `customer_enrichments` table with indexes on `customer_id` and `next_refresh_at`
- `tests/unit/test_customer_enrichment_model.py` — New file: unit tests verifying model instantiation and `to_dict()` serialization

## Implementation Steps
1. **Create `src/db/models/customer_enrichment.py`**: Define `CustomerEnrichmentModel` inheriting from `db.base.Base`, mirroring the pattern in `pipeline_stage.py`. Fields: `id` (Integer PK, autoincrement), `customer_id` (Integer, ForeignKey `customers.id`, nullable=False, index=True), `provider` (String(100), nullable=False), `raw_data_json` (JSON, default=dict, nullable=False), `enriched_at` (DateTime timezone=True, nullable=True), `next_refresh_at` (DateTime timezone=True, nullable=True, index=True). Add standard `created_at`/`updated_at` with `server_default=func.now()`. Implement `to_dict()` serializing all fields, with ISO format for datetimes.
2. **Create the Alembic migration**: Run `alembic revision --autogenerate -m "create_customer_enrichments"` targeting the `customers` table (revising from `c94d682d4b03`). Manually add an index on `next_refresh_at` (`ix_customer_enrichments_next_refresh_at`) alongside the autogenerated `customer_id` index. Delete any empty follow-up "drift_check" migration.
3. **Create `tests/unit/test_customer_enrichment_model.py`**: Add a `TestCustomerEnrichmentModel` class with `test_create_enrichment_defaults`, `test_to_dict`, and `test_customer_id_foreign_key` test methods following the style in `test_opportunity_model.py`.

## Test Plan
- Unit tests in `tests/unit/`: `test_customer_enrichment_model.py` — instantiates `CustomerEnrichmentModel` directly (no DB), asserts fields, enum-style values, and `to_dict()` output.
- Integration tests in `tests/integration/`: None required — the issue explicitly disclaims external API calls and limits scope to unit-level table-creation verification.

## Acceptance Criteria
- `src/db/models/customer_enrichment.py` imports cleanly with `python -c "from src.db.models import CustomerEnrichmentModel"` (PYTHONPATH=src).
- `alembic/versions/` contains a migration that `alembic upgrade head` applies without error against a live PostgreSQL DB.
- `alembic downgrade -1` reverses the migration cleanly.
- Unit test `pytest tests/unit/test_customer_enrichment_model.py -v` passes with zero errors.
- `ruff check src/db/models/customer_enrichment.py` and `mypy src/db/models/customer_enrichment.py` are clean.

## Risks / Open Questions
- Whether `raw_data_json` should use `JSON` (PostgreSQL JSON) or `JSONB` (binary, indexable) — the existing `AutomationRuleModel` uses `JSONB` for conditions/actions fields. If enrichment queries ever need to index inside the JSON payload, `JSONB` is preferred; otherwise `JSON` is sufficient. The migration should match the column type chosen in the model.
