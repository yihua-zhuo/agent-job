# Agent Task

- id: agent-1776089354841859310
- name: rewrite-trigger-workflow-report-
- description: Rewrite trigger + workflow + report + import_export services with PostgreSQL
- subagent_type: Agent
- created_at: 1776089354

## Prompt

Working directory: /home/node/.openclaw/workspace/dev-agent-system/.worktrees/auto-review

Rewrite FIVE service files to use PostgreSQL via SQLAlchemy async. Keep all method signatures and return types intact. Keep all business logic, enum values, and Chinese error messages. These services are mostly stateless/business-logic heavy — most just delegate to other services.

## File 1: src/services/trigger_service.py
- Keep `MarketingService` dependency injection pattern (self._marketing_service)
- Keep TRIGGERS dict and all existing methods: check_triggers, execute_trigger
- Methods are already async; just ensure correct ApiResponse handling (check .data, .status.value)
- No new DB tables needed — it calls marketing_service

## File 2: src/services/workflow_service.py
ORM model: `workflows` table (id, tenant_id, name, description, trigger_type, trigger_config JSON, actions JSON, conditions JSON, status, created_by, created_at, updated_at) and `workflow_executions` table (id, workflow_id, tenant_id, trigger_type, triggered_by, started_at, completed_at, status, result JSON).
Methods: create_workflow, get_workflow, update_workflow, activate_workflow, pause_workflow, delete_workflow, list_workflows, execute_workflow, evaluate_conditions, execute_actions, get_execution_history.
Note: `Workflow, WorkflowExecution, WorkflowStatus, TriggerType` classes already exist in `src/models/workflow.py` — keep importing from there and just replace in-memory storage.

## File 3: src/services/report_service.py
Simple methods: generate_pdf_report, generate_excel_report, export_to_csv, schedule_report. These are mostly stateless file-generation. No DB needed for report_service itself — it aggregates data from other services. Keep as-is but add `async def` where needed to fit the pattern.

## File 4: src/services/import_export_service.py
ORM models: `customers` (id, tenant_id, name, email, phone, company, status, owner_id, tags JSON, created_at, updated_at), `opportunities` (id, tenant_id, customer_id, name, stage, amount, probability, expected_close_date, owner_id, pipeline_id, created_at, updated_at). Methods: import_customers, import_opportunities, import_leads, export_customers, export_opportunities, export_report, generate_pdf_report. Keep the validation logic. Make DB writes async.

## File 5: src/services/churn_prediction.py
Keep all business logic (FACTOR_WEIGHTS, RISK_FACTORS, calculate_churn_score, predict_churn, get_churn_risk_factors, get_high_risk_customers, recommend_actions). The _get_mock_customer_data should eventually query DB but can stay as mock data. Keep `import hashlib` at top level. No DB changes needed for now — just keep the service as-is but clean.

For all files:
- Use `from src.db.connection import get_db_session` and `from sqlalchemy import select, update, delete, func, text, and_, or_`
- All methods become `async def`
- Use `async with get_db_session() as session` pattern

Write all five complete files. Each must parse without syntax errors.

## Result

- status: completed

### Final response

All five service files have been rewritten and pass syntax verification. Here's a summary of what was done:

---

## Changes Summary

### `trigger_service.py`
- `check_triggers` and `execute_trigger` → `async def`
- Added `await` calls to `self._marketing_service.list_campaigns(...)`, `get_campaign(...)`, and `record_event(...)`
- Kept `MarketingService` DI pattern, `TRIGGERS` dict, all business logic, and Chinese error messages

### `workflow_service.py`
- Removed all in-memory `_workflows` / `_executions` dicts and `_next_id` counter
- All 10 methods → `async def` using `async with get_db_session() as session`
- `create_workflow`, `update_workflow`, `activate_workflow`, `pause_workflow` → PostgreSQL INSERT/UPDATE via `sqlalchemy.dialects.postgresql.insert` with `.returning()` → reconstruct `Workflow` dataclass
- `list_workflows` → `select()` + `func.count()` with pagination
- `delete_workflow` → `delete()` returning `rowcount > 0`
- `execute_workflow` → persists `WorkflowExecution` to DB
- `get_execution_history` → `select()` ordered by `started_at.desc()`
- `evaluate_conditions` / `execute_actions` → `await` + re-fetch workflow from DB

### `report_service.py`
- All 4 methods → `async def` (stateless, no DB changes needed)
- Kept all business logic, file-generation placeholders, and Chinese comments

### `import_export_service.py`
- `import_customers`, `import_opportunities`, `import_leads` → `async def` with `pg_insert()` into `customers`/`opportunities` tables
- `export_customers`, `export_opportunities`, `export_report` → `async def` reading from DB via `select()` with `tenant_id` filter, falling back to sample data
- `generate_pdf_report` → `async def` (reportlab already used synchronously inside)
- All validation logic and Chinese error messages preserved
- Added `from src.db.connection import get_db_session`; added `re`, `Decimal`, `BytesIO` imports

### `churn_prediction.py`
- `import hashlib` moved to top-level (was inside `_get_mock_customer_data`)
- All business logic, `FACTOR_WEIGHTS`, `RISK_FACTORS`, and all method signatures kept as-is
- `calculate_churn_score`, `predict_churn`, `get_churn_risk_factors`, `get_high_risk_customers`, `recommend_actions` stay synchronous (CPU-only, no I/O)
- No DB changes; `_get_mock_customer_data` kept as-is
