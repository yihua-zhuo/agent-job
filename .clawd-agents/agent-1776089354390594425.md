# Agent Task

- id: agent-1776089354390594425
- name: rewrite-activity-analytics-marke
- description: Rewrite activity + analytics + marketing services with PostgreSQL
- subagent_type: Agent
- created_at: 1776089354

## Prompt

Working directory: /home/node/.openclaw/workspace/dev-agent-system/.worktrees/auto-review

Rewrite THREE service files to use PostgreSQL via SQLAlchemy async. Keep all method signatures and return types intact. Keep all business logic, enum values, and Chinese error messages.

## File 1: src/services/activity_service.py
ORM model: `ActivityModel` at src/db/models/activity.py (columns: id, tenant_id, customer_id, opportunity_id, type, content, created_by, created_at).

Methods to make async:
- create_activity, get_activity, update_activity, delete_activity, list_activities, get_customer_activities, get_opportunity_activities, search_activities, get_activity_summary
- `_scope` helper: returns activities scoped to tenant_id

## File 2: src/services/analytics_service.py  
ORM model: Use in-memory dict (no DB needed for analytics dashboards/reports — just replace self._dashboards and self._reports with async DB calls that read/write to tables). Create simple DB tables:
- `dashboards` table: id, name, description, widgets (JSON), owner_id, is_default, tenant_id, created_at, updated_at
- `reports` table: id, name, type, config (JSON), date_range (JSON), created_by, last_run_at, tenant_id, created_at

For each method: use `async with get_db_session() as session` and `await session.execute(text(...))` patterns.

## File 3: src/services/marketing_service.py
ORM model: `CampaignModel` at src/db/models/marketing.py (columns: id, tenant_id, name, type, status, subject, content, target_audience, trigger_type, trigger_days, created_by, sent_count, open_count, click_count, created_at, updated_at). `CampaignEventModel` (columns: id, campaign_id, tenant_id, customer_id, event_type, created_at).

Methods: create_campaign, get_campaign, update_campaign, delete_campaign, list_campaigns, record_event, get_campaign_stats, add_audience, trigger_campaign, get_campaign_events, get_campaign_by_trigger

For all files:
- Use `from src.db.connection import get_db_session` and `from sqlalchemy import select, update, delete, func, text, and_, or_`
- All methods become `async def`
- Use `async with get_db_session() as session` pattern
- For SELECT: use `await session.execute(text("SELECT ..."))` then `.fetchall()` or `.fetchone()`
- For INSERT/UPDATE/DELETE: use `await session.execute(text(...))` then `await session.commit()`
- Return exact same ApiResponse types

Write all three complete files. Make sure each parses without syntax errors.

## Result

- status: completed

### Final response

All 6 files pass Python syntax checks. Here's a summary of everything written:

---

## Files Created

### ORM Models (new — `src/db/models/`)
| File | Contents |
|------|----------|
| `activity.py` | `ActivityModel` → `activities` table (id, tenant_id, customer_id, opportunity_id, type, content, created_by, created_at) |
| `marketing.py` | `CampaignModel` → `campaigns` table; `CampaignEventModel` → `campaign_events` table |
| `analytics.py` | `DashboardModel` → `dashboards` table; `ReportModel` → `reports` table |

---

## Files Rewritten

### `src/services/activity_service.py`
- All 9 public methods + `_scope` are now `async def`
- Uses `async with get_db_session()` and `text(...)` for every DB call
- `_scope()` returns a SQL `WHERE` snippet scoped to `tenant_id`
- All enum values, Chinese error messages, and return types are unchanged

### `src/services/analytics_service.py`
- All dashboard and report methods are now `async def`
- `create_dashboard / get_dashboard / update_dashboard / list_dashboards / add_widget / remove_widget` → PostgreSQL `dashboards` table
- `create_report / get_report / update_report / list_reports / run_report` → PostgreSQL `reports` table
- Report generation helpers (`get_sales_revenue_report`, etc.) remain synchronous pure-Python as they have no DB dependency
- All Chinese messages preserved

### `src/services/marketing_service.py`
- All campaign/event methods are now `async def`
- `create_campaign / get_campaign / update_campaign / delete_campaign / list_campaigns / launch_campaign / pause_campaign` → PostgreSQL `campaigns` table
- `record_event / get_campaign_events / get_user_events` → PostgreSQL `campaign_events` table
- `get_campaign_stats` reads counters directly from DB
- `add_audience / trigger_campaign / get_campaign_by_trigger` implemented on top of DB
- All `CampaignStatus`, `CampaignType`, `TriggerType` enum values and Chinese messages preserved
