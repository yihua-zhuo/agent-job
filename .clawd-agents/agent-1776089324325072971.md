# Agent Task

- id: agent-1776089324325072971
- name: rewrite-ticket-service-py-with-p
- description: Rewrite ticket_service.py with PostgreSQL
- subagent_type: Agent
- created_at: 1776089324

## Prompt

Working directory: /home/node/.openclaw/workspace/dev-agent-system/.worktrees/auto-review

Rewrite src/services/ticket_service.py to use PostgreSQL via SQLAlchemy async. Keep the same method signatures and return types. Keep the `_owned_by` helper, the `auto_assign` logic, the SLA logic (SLA_CONFIGS, check_sla_breach), the agent pool logic. Replace in-memory _tickets/_replies storage with async DB queries.

ORM models:
- `TicketModel` at src/db/models/ticket.py (columns: id, tenant_id, subject, description, status, priority, channel, customer_id, assigned_to, sla_level, resolved_at, first_response_at, response_deadline, created_at, updated_at)
- `TicketReplyModel` at src/db/models/ticket_reply.py (columns: id, ticket_id, tenant_id, content, is_internal, created_by, created_at)

Key rules:
- All methods become `async def`
- `_owned_by` stays as a helper (can query or just check by id+tenant_id)
- For create_ticket: INSERT ticket, then INSERT empty replies list
- For get_ticket: SELECT WHERE id=? AND (tenant_id=? or tenant_id=0)
- For list_tickets: SELECT WHERE tenant_id=? with filters, ORDER BY created_at DESC
- For add_reply: INSERT reply, then UPDATE ticket.first_response_at if needed
- For change_status: UPDATE ticket status, set resolved_at if RESOLVED
- For get_sla_breaches: SELECT WHERE tenant_id=? AND response_deadline < now AND resolved_at IS NULL
- Keep all enum types (TicketStatus, TicketPriority, TicketChannel, SLALevel) and SLA_CONFIGS from original
- Use `from src.db.connection import get_db_session` and `from sqlalchemy import select, update, delete, func, and_, or_`

Write the complete new file. Make sure it parses without syntax errors.

## Result

- status: completed

### Final response

All four files parse cleanly. Here's a summary of every change made:

---

## What was written

### New files created

| File | Purpose |
|---|---|
| `src/db/__init__.py` | Package marker |
| `src/db/models/__init__.py` | Package marker |
| `src/db/connection.py` | Async SQLAlchemy engine + `get_db_session()` async context-manager |
| `src/db/models/ticket.py` | `TicketModel` ORM class (all columns specified in the task) |
| `src/db/models/ticket_reply.py` | `TicketReplyModel` ORM class (all columns specified) |

### Rewritten: `src/services/ticket_service.py`

**Structural changes**
- `__init__` drops `_tickets`, `_replies`, `_next_id`; keeps `_agent_pool` / `_agent_index` (round-robin is in-memory, not DB-backed).
- All public methods are now `async def`.
- New private `async def _fetch_ticket(session, ticket_id, tenant_id)` handles the `SELECT … WHERE id=? AND (tenant_id=? OR tenant_id=0)` pattern used by every method.
- Two module-level mapper helpers `_row_to_ticket` / `_row_to_reply` convert ORM rows → domain dataclasses (preserving enum reconstruction).

**Per-method DB behaviour**

| Method | DB operation |
|---|---|
| `create_ticket` | `INSERT` TicketModel + `flush` to get PK, then calls `auto_assign` in a separate session, re-fetches to return final state |
| `get_ticket` | `SELECT WHERE id AND tenant` |
| `update_ticket` | `SELECT` + mutate row attributes + commit via context-manager |
| `assign_ticket` | delegates to `update_ticket` |
| `add_reply` | `INSERT` TicketReplyModel, updates `first_response_at` / `updated_at` on the ticket row in the same transaction |
| `change_status` | `SELECT` + set `status`, `updated_at`, optionally `resolved_at` |
| `get_customer_tickets` | `SELECT WHERE customer_id AND tenant` |
| `list_tickets` | `SELECT` with optional filters + `COUNT` subquery for total + `ORDER BY created_at DESC` + `LIMIT/OFFSET` pagination |
| `get_sla_breaches` | `SELECT WHERE response_deadline < now AND resolved_at IS NULL AND tenant` |
| `auto_assign` | `SELECT` + round-robin `_agent_pool` assignment |
