# Agent Task

- id: agent-1776089322127502311
- name: rewrite-customer-service-py-with
- description: Rewrite customer_service.py with PostgreSQL
- subagent_type: Agent
- created_at: 1776089322

## Prompt

Working directory: /home/node/.openclaw/workspace/dev-agent-system/.worktrees/auto-review

Rewrite src/services/customer_service.py to use PostgreSQL via SQLAlchemy async. Keep the same method signatures and return types (ApiResponse). Use `async with get_db_session() as session` pattern. Use `from src.db.connection import get_db_session` and `from src.db.models.customer import CustomerModel` (the ORM model will be at src/db/models/customer.py with a `CustomerModel` class that has `id, tenant_id, name, email, phone, company, status, owner_id, tags, created_at, updated_at` columns). Use `from sqlalchemy import select, update, delete, func` and raw SQL/text for inserts.

Key rules:
- All methods become `async def`
- Replace in-memory dict/list operations with async DB queries
- For list methods: use `SELECT ... WHERE tenant_id = ? LIMIT ? OFFSET ?` with count query
- For create: use raw SQL INSERT with `RETURNING *` where possible, otherwise use ORM insert
- For update: use `UPDATE ... WHERE id=? AND tenant_id=?`
- For delete: use `DELETE FROM ... WHERE id=? AND tenant_id=?`
- Keep the exact same method signatures (same params, same return types)
- Keep tenant isolation checks as they are
- Add `from datetime import datetime` import
- Import `text` from sqlalchemy for raw SQL

Write the complete new file to src/services/customer_service.py. Make sure it parses without syntax errors. Keep the docstrings and error messages in Chinese as in the original.

## Result

- status: completed

### Final response

The file compiles cleanly. Here's a summary of what was rewritten:

---

## `src/services/customer_service.py` — Async PostgreSQL rewrite

**Imports added:**
- `from datetime import datetime`
- `from sqlalchemy import select, update, delete, func, text`
- `from src.db.connection import get_db_session`
- `from src.db.models.customer import CustomerModel`
- `from src.models.customer import Customer, CustomerStatus`

**Key changes per method:**

| Method | Pattern |
|---|---|
| `create_customer` | Raw `text(INSERT ... RETURNING *)` with `:tags::jsonb` |
| `list_customers` | Dynamic `WHERE` built from params + separate `COUNT(*)` query + `LIMIT/OFFSET` |
| `get_customer` | `SELECT * WHERE id=:id`, tenant isolation on dict after fetch |
| `update_customer` | Dynamic `UPDATE ... SET ... WHERE id=:id AND tenant_id=:tid RETURNING *` |
| `delete_customer` | `DELETE ... WHERE id=:id AND tenant_id=:tid RETURNING id` |
| `search_customers` | `SELECT * WHERE tenant_id=? AND (LOWER(name) LIKE ? OR LOWER(email) LIKE ?)` |
| `add_tag` / `remove_tag` | Fetch → parse JSONB tags → update → `json.dumps` not needed (list passed directly) |
| `change_status` / `assign_owner` | `UPDATE ... WHERE id=? AND tenant_id=? RETURNING *` + re-fetch for response |
| `bulk_import` | Per-row `INSERT ... RETURNING id` inside loop, each in its own `get_db_session()` |
| `count_by_status` | `SELECT status, COUNT(*) ... GROUP BY status` → `Dict[CustomerStatus, int]` |

**Helper:** `_row_to_dict(mapping)` — converts `RowMapping` → plain `dict`, normalises `status` enum value and ISO-formats `created_at`/`updated_at`.

**Removed:** `__init__`, `self._customers`, `self._next_id` — no in-memory state needed.
