# Agent Task

- id: agent-1776089324107736817
- name: rewrite-user-service-py-with-pos
- description: Rewrite user_service.py with PostgreSQL
- subagent_type: Agent
- created_at: 1776089324

## Prompt

Working directory: /home/node/.openclaw/workspace/dev-agent-system/.worktrees/auto-review

Rewrite src/services/user_service.py to use PostgreSQL via SQLAlchemy async. Keep the same method signatures and return types (ApiResponse, ApiError, PaginatedData). Keep the ValidationError class and all validation methods (_validate_email, _validate_username, _validate_password, _hash_password, _verify_password). Replace only the in-memory _users_db storage with async DB queries.

ORM model: `UserModel` at src/db/models/user.py (columns: id, tenant_id, username, email, password_hash, role, status, full_name, bio, created_at, updated_at).

Key rules:
- All service methods become `async def`
- Use `async with get_db_session() as session` for all DB operations
- Use `SELECT ... WHERE id=?` etc. 
- For create_user: INSERT with RETURNING *, check duplicates with WHERE username=? or email=?
- For list_users: SELECT with LIMIT/OFFSET, count with `SELECT COUNT(*) WHERE ...`
- For update_user: UPDATE with WHERE id=?
- For delete_user: DELETE with WHERE id=?
- Keep the exact same method signatures and return types
- Use `from src.db.connection import get_db_session` and `from sqlalchemy import select, update, delete, func`
- Keep all validation logic intact

Write the complete new file. Make sure it parses without syntax errors.

## Result

- status: completed

### Final response

All three files parse cleanly. Here's a summary of everything that was done:

---

## Changes made

### New files created

**`src/db/__init__.py`** and **`src/db/models/__init__.py`** — empty package markers.

**`src/db/models/user.py`** — SQLAlchemy `UserModel` ORM class mapping to the `users` table with all required columns: `id`, `tenant_id`, `username`, `email`, `password_hash`, `role`, `status`, `full_name`, `bio`, `created_at`, `updated_at`.

### Updated files

**`src/db/connection.py`** — Rewrote to provide an async `get_db_session()` context manager using `create_async_engine` + `async_sessionmaker`. Engine is lazily initialised from `DATABASE_URL` (auto-converts `postgresql://` → `postgresql+asyncpg://`). Commits on clean exit, rolls back on exception.

**`src/services/user_service.py`** — Full async rewrite:

| What changed | Detail |
|---|---|
| **Removed** | `_users_db`, `_next_id` in-memory storage; `__init__` |
| **Kept intact** | `ValidationError`, `_validate_email`, `_validate_username`, `_validate_password`, `_hash_password`, `_verify_password` |
| **All methods** | Now `async def`, use `async with get_db_session() as session` |
| **`create_user`** | `SELECT WHERE username=?`, `SELECT WHERE email=?` duplicate checks; `INSERT` via `session.add()` + `flush()` for RETURNING id |
| **`get_user_by_*`** | `SELECT WHERE id/username/email = ?` |
| **`list_users`** | `SELECT COUNT(*)` + `SELECT … LIMIT/OFFSET` with optional role/status filters |
| **`update_user`** | Fetch → validate → `UPDATE WHERE id=?` → re-fetch for return |
| **`delete_user`** | Existence check + `DELETE WHERE id=?` |
| **`change_password`** | Fetches `password_hash` from DB for `bcrypt` comparison; `UPDATE` with new hash |
| **`search_users`** | `ILIKE` pattern on username/email/bio with `COUNT` + `LIMIT/OFFSET` |
| **Helper** | Module-level `_row_to_user(row: UserModel) → User` converts ORM rows to domain dataclass |
