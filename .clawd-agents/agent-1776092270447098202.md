# Agent Task

- id: agent-1776092270447098202
- name: fix-sync-test-methods
- description: Fix sync test methods
- subagent_type: general-purpose
- created_at: 1776092270

## Prompt

Fix unit test files where tests are async but the service methods are synchronous. Do NOT run any tests.

For each file below, change `async def test_...` to `def test_...` and remove `await` from calls to methods that are NOT decorated with `@asynccontextmanager` and do NOT return a coroutine. Also remove `@pytest.mark.asyncio` from classes that contain only sync tests.

IMPORTANT rules to determine if a call needs `await`:
- If the service method is defined with `def` (not `async def`), it is sync — remove `await`
- If the service method is defined with `async def`, it is async — keep `await`
- If a call is to a MagicMock (mock), keep `await` only if it was originally `await`

Files to fix:

1. tests/unit/test_churn_prediction.py
2. tests/unit/test_auth_service.py — only fix `test_refresh_token_valid` and `test_refresh_token_invalid` which call `churn_service.calculate_churn_score(1)` (sync method — no await needed) and `auth_service.refresh_token(...)` (sync method — no await needed). The other auth tests can stay async if they call async service methods.

For test_churn_prediction.py specifically:
- ChurnPredictionService methods are all synchronous (plain `def`, no `async def`)
- Change ALL test methods from `async def` to `def`
- Remove ALL `await` keywords from service method calls
- Remove `@pytest.mark.asyncio` from the class decorators

For test_auth_service.py:
- Only fix `test_refresh_token_valid` and `test_refresh_token_invalid`
- These tests call `auth_service.refresh_token(refresh_token)` and `churn_service.calculate_churn_score(...)` — both are sync
- Change those two specific tests from `async def` to `def` and remove `await` from those calls

After fixing, verify by checking the file content. Do NOT run tests.

## Result

- status: failed

### Blocker

- failure_class: infra
- detail: context_window_blocked for claude-opus-4-6: estimated input 759482 + requested output 32000 = 791482 tokens exceeds the 200000-token context window; compact the session or reduce request size before retrying

### Error

context_window_blocked for claude-opus-4-6: estimated input 759482 + requested output 32000 = 791482 tokens exceeds the 200000-token context window; compact the session or reduce request size before retrying
