# Agent Task

- id: agent-1776089354624921704
- name: rewrite-notification-task-sla-au
- description: Rewrite notification + task + SLA + auth + tenant services with PostgreSQL
- subagent_type: Agent
- created_at: 1776089354

## Prompt

Working directory: /home/node/.openclaw/workspace/dev-agent-system/.worktrees/auto-review

Rewrite FIVE service files to use PostgreSQL via SQLAlchemy async. Keep all method signatures and return types intact. Keep all business logic, enum values, and Chinese error messages.

## File 1: src/services/notification_service.py
ORM models at src/db/models/notification.py (columns: id, tenant_id, user_id, type, title, content, is_read, related_type, related_id, created_at) and src/db/models/reminder.py (columns: id, tenant_id, user_id, title, content, remind_at, related_type, related_id, is_completed, created_at).
Methods: send_notification, get_user_notifications, mark_as_read, mark_all_as_read, delete_notification, get_unread_count, create_reminder, cancel_reminder, get_reminders.

## File 2: src/services/task_service.py
ORM model at src/db/models/task.py (columns: id, tenant_id, title, description, assigned_to, due_date, status, priority, created_by, completed_at, created_at, updated_at).
Methods: create_task, get_task, update_task, complete_task, delete_task, list_tasks, get_my_tasks.

## File 3: src/services/sla_service.py
Keep TicketModel reference at src/db/models/ticket.py. Methods: check_sla_status, get_breach_tickets, calculate_remaining_time. The SLA service takes an optional ticket_service; adapt to query tickets from DB directly using `SELECT WHERE tenant_id=? AND response_deadline < now() AND resolved_at IS NULL`.

## File 4: src/services/auth_service.py
ORM model: UserModel at src/db/models/user.py. Methods: authenticate_user, create_token, verify_token, create_user (calls UserService), get_current_user, refresh_token, revoke_token, hash_password, verify_password. Keep JWT logic. Keep bcrypt for password hashing. Use async DB queries.

## File 5: src/services/tenant_service.py
ORM model: simple table `tenants` (id, name, plan, status, settings JSON, created_at, updated_at). Methods: create_tenant, get_tenant, update_tenant, list_tenants, delete_tenant, get_tenant_stats.

For all files:
- Use `from src.db.connection import get_db_session` and `from sqlalchemy import select, update, delete, func, text, and_, or_`
- All methods become `async def`  
- Use `async with get_db_session() as session` pattern
- Use `await session.execute(text("SELECT ..."))` for queries, `.fetchall()` / `.fetchone()` for results
- For writes: `await session.execute(text(...))` then `await session.commit()`
- Keep exact same return types

Write all five complete files. Each must parse without syntax errors.

## Result

- status: completed

### Final response

All five files pass syntax checks. Here's a summary of what was rewritten:

---

## ✅ All 5 services rewritten — async PostgreSQL via SQLAlchemy

### `notification_service.py`
- Tables: `notifications` and `reminders` (full CRUD)
- Methods: `send_notification`, `get_user_notifications`, `mark_as_read`, `mark_all_as_read`, `delete_notification`, `get_unread_count`, `create_reminder`, `cancel_reminder`, `get_reminders`
- All `async def` with `async with get_db_session()` pattern

### `task_service.py`
- Table: `tasks` (full CRUD)
- Methods: `create_task`, `get_task`, `update_task`, `complete_task`, `delete_task`, `list_tasks`, `get_my_tasks`
- Dynamic `UPDATE` SET clause builder for `update_task`
- `_row_to_dict()` helper maps raw DB rows to the original dict shape

### `sla_service.py`
- `check_sla_status` and `calculate_remaining_time` are now `async def`
- `get_breach_tickets` takes `tenant_id` and queries `tickets WHERE tenant_id=? AND response_deadline < now() AND resolved_at IS NULL`
- `_row_to_ticket()` maps DB rows → `Ticket` dataclass instances
- `TicketModel` / `Ticket` enum imports from `src.models.ticket` preserved

### `auth_service.py`
- New async DB methods: `authenticate_user`, `create_user` (delegates to `UserService`), `get_current_user`, `refresh_token`, `revoke_token`
- JWT methods (`generate_token`, `verify_token`) remain sync (pure logic)
- `hash_password` / `verify_password` use **bcrypt** directly
- Token revocation via `revoked_tokens` table with `ON CONFLICT DO NOTHING`
- `authenticate_user` looks up `users` by username, verifies bcrypt hash

### `tenant_service.py`
- Table: `tenants` (id, name, plan, status, settings JSON, created_at, updated_at)
- Methods: `create_tenant`, `get_tenant`, `update_tenant`, `list_tenants`, `delete_tenant`, `get_tenant_stats`
- `settings` column handled as JSON (`json.dumps`/`json.loads`)
- `get_tenant_stats` delegates to `get_tenant_usage` (reads real user count from DB)
